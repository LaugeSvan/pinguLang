<?php
// 1. Database Configuration
define('DB_HOST', 'localhost'); // Change to your database
define('DB_USER', 'roott'); // Change if your DB uses a different user
define('DB_PASS', ''); // Change if your DB has a password
define('DB_NAME', 'name'); //Change to the name of your DB

// 2. AI Configuration (Groq API)
define('GROQ_API_KEY', ''); define('', ''); // This is your Groq API Key, get one on groq.com

// 3. Admin Page Password
define('ADMIN_PASSWORD', 'YourSuperSecretPassword123'); // Change to what you want your admin password to be

// Create Connection
$conn = new mysqli(DB_HOST, DB_USER, DB_PASS, DB_NAME);
if ($conn->connect_error) { die("Connection failed: " . $conn->connect_error); }
?>
<!-- Quick reminder to the user not to be here -->
<p>What are you doing here?</p>